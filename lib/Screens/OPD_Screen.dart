import 'package:flutter/material.dart';

class OpdScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("OPD Screen"),
      ),
    );
  }
}
